% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidydata.R
\name{tidydata}
\alias{tidydata}
\title{Tidy Continuous Glucose Monitoring Data}
\usage{
tidydata(
  inputdir,
  outputdir = tempdir(),
  skiphours = 0,
  gapprocess = TRUE,
  maxgapmin = 30,
  shortgapsm = "linear",
  longgapsm = "spline",
  trimdays = FALSE,
  logdisplay = TRUE,
  unit = "mg/dL"
)
}
\arguments{
\item{inputdir}{Character. Directory containing raw CGM files to tidy.}

\item{outputdir}{Character. Directory where cleaned CSV files will be written.
Defaults to \code{tempdir()}.}

\item{skiphours}{Numeric. Number of hours to skip at the beginning of each
file. Defaults to 0.}

\item{gapprocess}{Logical. Whether to detect and handle missing glucose
segments. If \code{TRUE}, short and long gaps are processed according to
\code{shortgapsm} and \code{longgapsm}. If \code{FALSE}, missing values are preserved.}

\item{maxgapmin}{Numeric. Maximum gap length (in minutes) treated as a short
gap for interpolation. Defaults to 30.}

\item{shortgapsm}{Character. Method for filling short gaps (only used when
\code{gapprocess = TRUE}). Supported values are \code{"linear"} (default) and
\code{"spline"}.}

\item{longgapsm}{Character. Method for handling long gaps (only used when
\code{gapprocess = TRUE}). Supported values are \code{"spline"} (default),
\code{"dailymean"}, and \code{"remove"}.}

\item{trimdays}{Logical. Whether to trim data to complete 24-hour periods.
If \code{TRUE}, partial leading and trailing days are removed. Defaults to
\code{FALSE}.}

\item{logdisplay}{Logical. Whether to print progress messages for each file.
Defaults to \code{TRUE}.}

\item{unit}{Character. Output glucose unit. Supported values are \code{"mg/dL"}
(default) and \code{"mmol/L"}. If conversion is needed, \code{"mg/dL"} is converted to
\code{"mmol/L"} by dividing by 18, and \code{"mmol/L"} is converted to \code{"mg/dL"} by
multiplying by 18.}
}
\value{
Invisibly returns a named list of skipped/failed file names:
\itemize{
\item \code{skipped_few_glucose}: files with fewer than 10 non-missing glucose values.
\item \code{skipped_trim_empty}: files that became empty after trimming.
\item \code{skipped_less_than_1day}: files shorter than 24 hours when \code{trimdays = TRUE}.
\item \code{skipped_bad_interval}: files with invalid or undeterminable sampling intervals.
\item \code{skipped_skiphours_too_short}: files whose total duration is <= \code{skiphours}.
\item \code{skipped_errors}: files that failed due to unexpected errors.
}
}
\description{
\code{tidydata()} streamlines pre-processing of CGM datasets for downstream
analyses. It detects device-specific export formats, standardizes column
names, parses timestamps, performs optional gap processing, and writes cleaned
CSV files. Unless manually pre-processed (e.g., following \code{metricsstat()}),
input files should remain in their original export formats. For Diasend and
Sibionics Excel files, CGM data must be stored in the first worksheet/tab.
}
\details{
Read raw CGM exports from multiple devices and write standardized,
analysis-ready CSV files with consistent columns and timestamps. The function
optionally processes missing glucose segments (short-gap interpolation and
long-gap handling) and can trim recordings to complete 24-hour days.

The function performs the following steps:
\enumerate{
\item Read raw CGM files from \code{inputdir} (recursively).
\item Parse timestamps and standardize to two core columns:
\code{timestamp} and \code{sensorglucose}.
\item Optionally skip the initial \code{skiphours} hours of data.
\item Optionally process gaps when \code{gapprocess = TRUE}:
\itemize{
\item Short gaps (<= \code{maxgapmin} minutes) are filled using \code{shortgapsm}
(\code{"linear"} or \code{"spline"}).
\item Long gaps (> \code{maxgapmin} minutes) are handled using \code{longgapsm}
(\code{"spline"}, \code{"dailymean"}, or \code{"remove"}).
}
\item Detect glucose units from the observed range and convert to \code{unit}
(default \code{"mg/dL"}). Internally, downstream analyses in this package assume
mg/dL.
\item Optionally trim to complete 24-hour periods when \code{trimdays = TRUE}.
\item Write standardized CSV files to \code{outputdir}.
}

Files are skipped and reported if they have too few non-missing glucose values
(< 10), if trimming results in an empty dataset, if \code{trimdays = TRUE} but the
total duration is < 24 hours, if the sampling interval cannot be determined,
or if the recording is shorter than or equal to \code{skiphours}.
}
\examples{
\dontrun{
tidydata(
  inputdir = "rawdata",
  outputdir = "tidydata",
  skiphours = 0,
  gapprocess = TRUE,
  maxgapmin = 30,
  shortgapsm = "linear",
  longgapsm = "spline",
  trimdays = FALSE,
  logdisplay = TRUE,
  unit = "mg/dL"
)
}

}
\seealso{
\code{\link[=metricsstats]{metricsstats()}}, \code{\link[=agpanalyze]{agpanalyze()}}
}
